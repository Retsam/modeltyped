// Main entry point for the project, controls no files, but specifies all of the sub-projects.
{
    "extends": "./tsconfig-base",
    "references": [
        {"path": "./lib"},
        {"path": "./tests"}
    ],
    "files": [],
    "compilerOptions": {"declaration": true} // for rollup
}
